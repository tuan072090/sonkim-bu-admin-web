import React from "react";
import {Layout} from "../../components";

const NotificationsPage = Layout(() => {

    return (
        <div>Notifications Page</div>
    )
})

export default NotificationsPage
